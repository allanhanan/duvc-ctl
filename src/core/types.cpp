/**
 * @file types.cpp
 * @brief Core type implementations
 */

#include <duvc-ctl/core/types.h>

namespace duvc {

// PropRange method implementations are already in header (inline)
// Device method implementations are already in header (inline)

// No additional implementation needed for basic types

} // namespace duvc
