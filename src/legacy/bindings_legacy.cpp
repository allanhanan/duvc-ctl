/**
 * @file bindings_legacy.cpp
 * @brief Legacy C bindings support using new modular implementation
 */

#include <duvc-ctl/c/api.h>

// This file provides legacy bindings support.
// The actual C API implementation is in src/c/api.cpp which uses the new modular core.
// This file exists as a placeholder for any legacy-specific C binding compatibility.

// No additional implementation needed - the C API in src/c/api.cpp provides
// the same interface as the original bindings.cpp but using the new modular core.
